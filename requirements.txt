# Core library dependencies: everything needed for the actual matching
# logic (perceptual hashing, padding detection).
Pillow>=10
imagehash>=4.3
numpy>=1.24
#
# The GCS client and ffprobe/ffmpeg themselves stay duck-typed/pluggable
# (see gcs_signing.py, video_probe.py) -- no google-cloud-storage import
# is required unless you opt into the "gcs" extra:
#   pip install -e .[gcs]
#
# To run the HTTP API, install the "api" extra instead:
#   pip install -e .[api]
#
# ffprobe/ffmpeg themselves are system binaries, not Python packages --
# install via your OS package manager (e.g. `apt install ffmpeg` on
# Debian/Ubuntu, `brew install ffmpeg` on macOS), since they ship as
# part of the ffmpeg distribution.
